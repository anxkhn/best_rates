[project]
name = "best-rates"
version = "0.1.0"
description = "Visa and Mastercard endpoints to compare currency rates"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi>=0.15.0",
    "matplotlib>=3.11.0",
]
