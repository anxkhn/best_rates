[project]
name = "best-rates"
version = "0.1.0"
description = "Compare Visa vs Mastercard card conversion rates before an international trip"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi>=0.15.0",
    "matplotlib>=3.11.0",
]
